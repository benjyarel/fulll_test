### How to Run FizzBuzz

```
 npm start <number>
 npm test
```

### Notes

#### IA usage

As long this exercise is a classical "FizzBuzz" algo, it won't make sense to use much of IA in this context.
I used Claude Code for two things only : Quickly setup the package.json and how to catch user input on the terminal.

#### Other

I choosed to directly log on the terminal each number instead of returning an array within `fizzBuzz.ts` as the readme ask only to display the number.
